'use strict'

const contextOpts = {
  issue: 'https://issues-repository.com/browse', // must be the full url
  host: 'https://host.com',
  owner: 'owner',
  repository: 'repository',
  commit: 'commits'
}

module.exports = contextOpts
